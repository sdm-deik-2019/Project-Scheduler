https://www.mysql.com/why-mysql/windows/

create table db.tb3
(
fromd  varchar(30),
tod varchar(30),
body varchar(100)
);

The backend will work regardless how you name the 'fromd', 'tod' and 'body', but the schema and the table should be db.tb3